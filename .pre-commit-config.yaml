repos:
  - repo: https://github.com/jorisroovers/gitlint
    rev: v0.19.1  # Use the version that matches your installed Gitlint version
    hooks:
      - id: gitlint
        stages: [commit-msg]

  - repo: https://github.com/golangci/golangci-lint
    rev: v2.4.0
    hooks:
      - id: golangci-lint
        args: [--timeout=5m]